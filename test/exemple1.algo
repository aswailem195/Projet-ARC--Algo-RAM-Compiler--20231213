PROGRAMME()
VAR toto, titi, tutu ,t[5]
VAR  somme , @m
VAR momo[5]
DEBUT
	5+9/3-7
	39+2
	ECRIRE( somme )
	o <- LIRE()
	l <- LIRE()
	TQ i < taille FAIRE
	   l <- LIRE()
	   i <- i+1
	FTQ
	i <- i+1
	TQ i < n FAIRE
	   SI d > d ALORS
	      imin <- i
	   SINON 
	   	l <- 5
	   FSI
	   i <- i+1
	FTQ


FIN
